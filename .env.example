NEXTAUTH_URL=http://localhost:3000
# REQUIRED in production — the app refuses to start without it. Generate a
# strong, random value, e.g.:  openssl rand -base64 32
NEXTAUTH_SECRET=local-dev-secret-123
